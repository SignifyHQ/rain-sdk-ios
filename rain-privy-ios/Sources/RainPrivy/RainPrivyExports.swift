// RainPrivy re-exports RainCore so a single `import RainPrivy` gives the full SDK surface
// (`RainSdk`, `RainClient`, models, errors) — the same convention as the other adapter modules.
@_exported import RainCore
